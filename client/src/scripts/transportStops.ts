import Client from "../Client";

// Stop patterns were previously stored in JSON files and used to register
// triggers. Those triggers are removed, so we no longer need to load the data.

export default function initTransportStops(_client: Client) {
    // Stop patterns triggers were removed.
}
